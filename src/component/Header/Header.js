import React from "react";
import "./Header.css";

const Header = () => {

    return(
        <header className="headerBox">
            <h1 className="text mainText">Да, да это просто статичный текст</h1>
            <h3 className="text subText">(здесть правда ничего не произойдёт)</h3>
        </header>
    )
}

export default Header;